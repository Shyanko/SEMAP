# SEMAP 实现日志

## 2026-07-02 14:56

- 按确定性交付范围重写 `AGENTS.md`，固定为 Android App + FastAPI 后端 + PostgreSQL + Nginx。
- 安装 Git、Nginx、Node.js 20、Java 17、PostgreSQL 13、Python 3.11、Android SDK command line tools、Android Platform 35、Android Build Tools 35.0.0。
- 初始化 PostgreSQL，创建 `semap` 数据库和 `semap` 数据库用户。
- 新增 `.env.example`、本机 `.env` 和 `.gitignore`。
- 创建 FastAPI 最小后端，提供 `/health` 和 `/api/health`。
- 创建 `semap-backend` systemd 服务并设置开机自启。
- 将 Nginx 默认 80 端口配置为反向代理到后端服务。
- 遇到 Python 3.6 依赖版本过旧，改为安装 Python 3.11 并重建虚拟环境。
- 遇到 PostgreSQL `ident` 认证导致健康检查失败，将本机 TCP 认证调整为 `md5`。
- 验证 `http://127.0.0.1:8000/health` 和 `http://127.0.0.1/health` 均返回数据库正常。

## 2026-07-02 15:04

- 用户明确 Web 前端也需要开发。
- 更新 `AGENTS.md`，将交付范围固定为 Web 前端 + Android App + 后端 API + PostgreSQL。
- 新增 `frontend/`，采用 React + TypeScript + Vite。
- 创建 Web 前端底座，包含侧边导航、健康检查状态、轨迹地图占位区域和交付范围面板。
- npm 镜像没有 `typescript@5.9.4`，查询后改用镜像可用的 TypeScript 6.0.3、Vite 8.1.3、React 19.2.7。
- TypeScript 6 对旧 `moduleResolution: Node` 报错，改为 `Bundler`。
- 构建缺少 React 和 Node 类型声明，安装 `@types/react`、`@types/react-dom`、`@types/node`。
- `npm run build` 已通过。
- 将 Web 构建产物部署到 `/var/www/semap`。
- 调整 Nginx，`/` 服务 Web 静态文件，`/api/` 和 `/health` 代理到 FastAPI。
- 验证 `http://127.0.0.1/`、`http://127.0.0.1/api/health` 和 `http://127.0.0.1/health` 均正常。

## 2026-07-02 15:06

- 确认服务器已安装 Git 2.43.7。
- 将 `api/` 加入 `.gitignore`，避免明文 API 配置进入仓库。
- 在 `/root/semap` 执行 `git init` 初始化仓库。
- 将默认分支从 `master` 改名为 `main`。
- 验证 `.env`、`api/`、`.venv/`、`frontend/node_modules/` 和 `frontend/dist/` 均被 Git 忽略。

## 2026-07-02 15:25

- 用户确认公网登录需要 HTTPS、阶段顺序需要修正、迁移采用精简 SQL 脚本、轨迹删除版本放在 query 参数、GPS 会话开始时创建轨迹片段。
- 用户确认不提供普通轨迹点批量写入接口，点位写入由手动创建、外部导入和 GPS 会话各自负责。
- 用户确认 FlightRadar24 当前为 Explorer 计划，航班导入开发前需要验证 token 可访问的接口范围。
- 验证 `https://kyfw.12306.cn/otn/queryTrainInfo/init` 可访问，页面返回当前日期配置。
- 直接使用猜测的 `train_no` 调用 `queryTrainInfo/query` 返回空数据。
- 使用 `leftTicket/queryG` 查询 2026-07-02 北京到上海方向车次成功，响应中包含内部 `train_no`。
- 使用 `train_no=240000G80310` 调用 `queryTrainInfo/query` 成功返回 `G803` 经停站列表。
- 验证 `https://search.12306.cn/search/v1/train/search?keyword=G803&date=20260702` 可直接按车次号返回内部 `train_no`。
- 将火车导入计划改为先按 `trainCode` 搜索并精确匹配 `station_train_code`，再查询经停站，避免在请求字段中增加出发站和到达站。
- 更新 `AGENTS.md`，明确 12306 两步解析、轨迹版本规则、GPS 会话生命周期、阶段编号和 HTTPS 要求。
- 更新 `README.md`，补充当前状态、后端接口约束和外部服务验证结论。

## 2026-07-02 15:50

- 开始阶段 2 后端数据模型和账号能力。
- 对照 FastAPI 官方安全文档，保留项目约定的 JSON 登录注册接口，使用 Bearer JWT 作为后续鉴权方式。
- 安装 `PyJWT==2.10.1`、`pytest==9.0.2` 和 `pwdlib[argon2]==0.3.0`。
- 新增精简 SQL 迁移 `backend/migrations/001_accounts.sql`，创建 `accounts` 表。
- 新增迁移执行入口 `python -m app.migrate`，使用 `semap_migrations` 记录已执行脚本。
- 新增数据库连接模块、密码哈希模块和 JWT 模块。
- 密码哈希使用 FastAPI 官方文档当前推荐的 `pwdlib[argon2]`。
- 新增 `POST /api/auth/register`、`POST /api/auth/login`、`GET /api/auth/me`。
- 注册重复用户名返回中文 `409`，缺少 token 返回中文 `401`。
- 新增后端测试，覆盖注册、登录、当前用户接口、重复注册和未登录访问。
- 执行 `python -m app.migrate` 成功。
- 执行 `python -m pytest -q` 通过，结果为 `3 passed`。测试过程中出现 FastAPI TestClient 上游弃用警告，不影响当前接口。
- 重启 `semap-backend` 后，通过 Nginx `/api/` 入口验证注册、登录和当前用户接口成功。
- 第一次真实 HTTP 验证脚本未禁用环境代理，拿到非 JSON 响应；随后改用 `httpx.Client(trust_env=False)` 验证通过。
- 清理真实 HTTP 验证使用的临时账号。
- 更新 `.gitignore`，忽略 `.pytest_cache/`。
- 复核密码哈希标准实现后，将最初写入的 PBKDF2 实现替换为 Argon2。当前没有正式账号数据，不保留旧哈希兼容路径。

## 2026-07-02 16:05

- 开始阶段 3 轨迹基础能力。
- 新增迁移 `backend/migrations/002_tracks.sql`，创建 `track_segments` 和 `track_points`。
- `track_segments.account_id` 外键关联 `accounts.id`，账号删除后级联删除轨迹。
- `track_points.segment_id` 外键关联 `track_segments.id`，轨迹删除后级联删除点位。
- 新增 `GET /api/segments`、`POST /api/segments`、`GET /api/segments/{segmentId}`、`PATCH /api/segments/{segmentId}`、`DELETE /api/segments/{segmentId}?version={version}`。
- `POST /api/segments` 只创建 `manual` 来源轨迹，并一次性写入请求中的点位。
- 轨迹列表和详情返回点位数组，供 Web 和 Android 地图绘制使用。
- 所有轨迹查询、更新和删除都带 `account_id` 条件，访问其他用户轨迹返回 `404`。
- `PATCH` 请求只修改请求体中出现的字段，未出现字段保持原值。
- `PATCH` 和 `DELETE` 版本不一致时返回中文 `409`。
- 新增轨迹测试，覆盖创建、列表、详情、编辑、删除、用户隔离和版本冲突。
- 执行 `python -m app.migrate` 成功，数据库迁移记录包含 `001_accounts.sql` 和 `002_tracks.sql`。
- 执行 `python -m pytest -q` 通过，结果为 `6 passed`。测试仍有 FastAPI TestClient 上游弃用警告。
- 重启 `semap-backend` 后，通过 Nginx `/api/` 入口验证创建轨迹、编辑轨迹、旧版本删除冲突和正确版本删除成功。
- 清理真实 HTTP 验证使用的临时账号，级联清理临时轨迹和点位。

## 2026-07-02 16:20

- 用户明确手动轨迹创建功能不应保留。
- 删除后端 `POST /api/segments` 手动创建接口。
- 删除后端手动轨迹创建请求模型和点位写入逻辑。
- 调整轨迹测试，不再通过公开接口创建测试轨迹，改为测试内直接插入外部导入形态的轨迹数据。
- 新增测试确认 `POST /api/segments` 返回 `405`。
- 更新 `AGENTS.md`，从 Web、Android 和后端首版范围中移除手动新增轨迹。
- 更新 `README.md`，明确普通轨迹片段不提供通用创建接口，轨迹由航班导入、火车导入和 GPS 会话创建。
- 移除 Web 前端占位界面中的“新增轨迹”按钮。
- 调整 `002_tracks.sql` 中 `source_type` 约束，首版只保留 `flight`、`train`、`gps`。
- 新增 `003_remove_manual_segments.sql`，用于修正当前服务器已应用的数据库约束。
- 并行执行迁移和测试时触发 `semap_migrations` 记录竞争，随后给迁移器增加 PostgreSQL advisory lock，并将迁移记录插入改为 `on conflict do nothing`。
- 重新执行 `python -m app.migrate && python -m pytest -q` 通过，结果为 `7 passed`。
- 执行 `npm run build` 通过，并更新 `/var/www/semap/` 静态文件。
- 重启 `semap-backend` 后验证 `POST /api/segments` 返回 `405`。
- 验证数据库 `track_segments_source_type_check` 只允许 `flight`、`train`、`gps`。

## 2026-07-02 16:45

- 继续阶段 4 Web 前端基础框架。
- 读取 `react-best-practices`、`frontend-app-builder` 和 `frontend-design` 技能说明。当前任务是在既有前端中补基础业务框架，未使用图像生成概念稿。
- 新增 `frontend/src/api.ts`，封装同源 `/api` 请求、错误解析、账号接口、轨迹接口、编辑接口和删除接口。
- 新增 `frontend/src/types.ts`，集中定义账号、轨迹、点位、健康状态和工作区视图类型。
- 重写 `frontend/src/main.tsx`，实现登录、注册、token 持久化、启动恢复登录、当前用户读取、轨迹列表拉取、退出登录、轨迹选择、详情编辑和删除。
- 导入面板只保留航班和火车的表单框架，提交按钮保持禁用，等待后端导入接口。
- 地图区域当前是稳定布局和轨迹预览占位，Google Maps 接入留到阶段 5。
- 重写 `frontend/src/styles.css`，补齐登录页、工作区、侧边导航、地图区域、轨迹列表、详情编辑和导入表单的响应式样式。
- 执行 `npm run build` 通过。
- 执行 `python -m pytest -q` 通过，结果为 `7 passed`。测试仍有 FastAPI TestClient 上游弃用警告。
- 将 Web 构建产物部署到 `/var/www/semap/`。
- 验证 `http://127.0.0.1/` 返回当前静态入口，`http://127.0.0.1/api/health` 正常。
- 用户要求补充浏览器验证能力后，安装 `@playwright/test`，执行 `npx playwright install chromium` 安装 Chromium。
- Playwright 提示 Alibaba Cloud Linux 3 不是官方支持系统，已下载 Ubuntu 24.04 fallback build。
- 第一次截图发现中文显示为方块，确认系统缺少中文字体。
- 安装 `google-noto-sans-cjk-ttc-fonts`，重新截图后中文显示正常。
- 使用 Playwright 打开 `http://127.0.0.1/`，验证页面标题、登录页内容、注册登录流程、空轨迹工作区、航班面板切换和移动视口布局。
- Playwright 控制台错误和页面错误记录为空。
- 截图文件保存在 `/tmp/semap-stage4-desktop.png`、`/tmp/semap-stage4-workspace.png`、`/tmp/semap-stage4-mobile.png`。
- 清理 Playwright 验证创建的临时账号，当前 `accounts` 数量为 `0`。

## 2026-07-02 17:10

- 用户明确前端风格要求：卡片尽量少使用色块填充，白底即可。
- 更新 `AGENTS.md` 和 `README.md`，记录 Web 前端视觉约束。
- 调整 Web 样式，登录页、分段控件、状态条、轨迹选中项和地图预览减少浅色块填充。
- 轨迹选中状态改为边框和左侧强调线，轨迹类型标签改为白底细边框。

## 2026-07-02 17:42

- 开始阶段 5 Web 地图展示。
- 对照 Google Maps JavaScript API 当前文档，采用 `@googlemaps/js-api-loader` v2 的 `setOptions` 和 `importLibrary` 加载方式。
- 移除未使用的 `@react-google-maps/api`，直接使用 Google Maps JavaScript API 创建 `Map`、`AdvancedMarkerElement`、`Polyline` 和 `LatLngBounds`。
- 新增 `VITE_GOOGLE_MAPS_MAP_ID` 示例配置，供 Web Advanced Marker 使用。
- 新增 `frontend/src/googleMaps.ts` 和 `frontend/src/TrackMap.tsx`。
- Web 轨迹地图从静态预览改为真实地图组件，轨迹点使用 Advanced Marker，轨迹线使用 Polyline，选中轨迹使用更高层级和强调色。
- 地图视野优先适配选中轨迹，没有选中轨迹时适配全部轨迹点。
- 调整 Vite `envDir`，让前端构建读取仓库根目录 `.env`，与项目配置文档保持一致。
- 当前服务器 `VITE_GOOGLE_MAPS_API_KEY` 仍是占位值，真实 Google 地图瓦片没有完成验收，页面会显示配置缺失提示。
- 修正轨迹详情时间字段在侧栏内横排导致裁切的问题，改为单列展示。
- 执行 `npm run build` 通过。
- 执行 `python -m pytest -q` 通过，结果为 `7 passed`。
- 将 Web 构建产物部署到 `/var/www/semap/`。
- 使用 Playwright 创建临时账号并插入北京到上海测试轨迹，验证登录、轨迹列表、地图配置缺失状态、桌面和移动视口截图。
- Playwright 控制台错误和页面错误记录为空。
- 截图文件保存在 `/tmp/semap-stage5-map-config-state.png` 和 `/tmp/semap-stage5-map-mobile.png`。
- 清理 Playwright 验证创建的临时账号，当前 `accounts` 数量为 `0`。

## 2026-07-02 18:05

- 按用户要求将 `api/googlemap.txt` 中的 Maps Platform API Key 写入本机 `.env` 的 `GOOGLE_MAPS_API_KEY` 和 `VITE_GOOGLE_MAPS_API_KEY`。
- 未在终端输出和文档中记录密钥内容。
- 重新执行 `npm run build`，构建产物已部署到 `/var/www/semap/`。
- 执行 `python -m pytest -q` 通过，结果为 `7 passed`。
- 使用 Playwright 创建临时账号并插入北京到上海测试轨迹，确认 Google Maps 页面加载成功，`.gm-style` 存在，缺 key 提示不再显示。
- 将 Advanced Marker 点击监听从旧事件改为 `gmp-click`，消除 Google Maps 控制台警告。
- Playwright 控制台错误和警告记录为空。
- 截图文件保存在 `/tmp/semap-stage5-google-map.png` 和 `/tmp/semap-stage5-google-map-mobile.png`。
- 清理 Playwright 验证创建的临时账号，当前 `accounts` 数量为 `0`。

## 2026-07-02 17:16

- 开始阶段 6 Android App 基础框架。
- 按用户要求直接升级缺失构建包，安装 Android SDK Platform 36、Android Build Tools 36.0.0 和 Gradle 9.6.1。
- 创建 `android/` 原生 Android 工程，使用 AGP 9.2.1、Kotlin Compose 插件 2.4.0、Jetpack Compose BOM 2026.06.01。
- 使用 AGP 9 内置 Kotlin 支持，没有使用旧的 `org.jetbrains.kotlin.android` 插件。
- 新增 Gradle Wrapper，后续 Android 构建使用 `android/gradlew`。
- 新增 Compose 登录、注册和轨迹列表页面。
- 新增 Retrofit + OkHttp + Kotlinx Serialization 网络层。
- 新增 DataStore token 持久化，启动后读取 token 并恢复登录状态。
- Android API base URL 通过 Gradle 属性 `SEMAP_API_BASE_URL` 注入，默认值为 Android 模拟器访问本机的 `http://10.0.2.2/api/`。
- 新增 Android 网络安全配置，只允许模拟器本机地址 `10.0.2.2` 使用明文 HTTP，公网接口仍按项目要求使用 HTTPS。
- 引入 Google Maps SDK for Android，地图绘制留到阶段 7。
- 没有引入 Room，离线定位点缓存留到阶段 9。
- 第一次 Android 构建因为 Compose `Column` 函数误写成 receiver 类型失败，改为 `ColumnScope`。
- 第二次 Android 构建在 dex 阶段触发 OOM，确认服务器没有 swap。
- 启用 `/swapfile-semap` 作为 4GiB 构建 swap，并将 Gradle worker 限制为 1、JVM heap 降到 1024m。
- `lifecycle-runtime-compose:2.11.0` 要求 `compileSdk 37`，当前 SDK 仓库没有 `platforms;android-37`，改用稳定版 `2.10.0` 保持 `compileSdk 36`。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 成功，APK 位于 `android/app/build/outputs/apk/debug/app-debug.apk`，大小约 14 MiB。
- 执行 `python -m pytest -q` 通过，结果为 `7 passed`。

## 2026-07-02 19:33

- 为 `semap.xyz` 配置公网 HTTPS 访问。
- 安装 `certbot` 和 `python3-certbot-nginx`。
- 新增 Nginx 域名站点配置 `/etc/nginx/conf.d/semap.conf`，保留 `/` 静态文件、`/api/` 后端代理和 `/health` 健康检查代理。
- 首次签发前 `semap.xyz` 解析为 NXDOMAIN，确认阻塞点是 DNS 未生效。
- 域名解析生效后确认 `semap.xyz` 指向当前服务器公网 IP `8.217.54.207`。
- 使用 Certbot 为 `semap.xyz` 签发 Let's Encrypt 证书，并启用 HTTP 到 HTTPS 自动跳转。
- 当前证书路径为 `/etc/letsencrypt/live/semap.xyz/fullchain.pem` 和 `/etc/letsencrypt/live/semap.xyz/privkey.pem`，到期时间为 2026-09-30。
- 确认 `certbot-renew.timer` 已启用，证书会自动续期。
- 验证 `http://semap.xyz/` 返回 `301` 跳转到 HTTPS。
- 验证 `https://semap.xyz/` 返回 Web 前端静态入口。
- 验证 `https://semap.xyz/api/health` 返回 `{"status":"ok","database":"ok"}`。
- 验证 Nginx 配置语法通过，80、443 和后端 8000 端口均处于监听状态。

## 2026-07-02 17:28

- 用户反馈本地访问空轨迹状态下看不到地图。
- 确认 Web 地图组件在空轨迹时会用白色“暂无轨迹”状态层覆盖 Google 地图底图。
- 调整 Web 地图空状态：Google Maps 加载成功后始终显示底图，空轨迹提示改为地图左上角小提示。

## 2026-07-02 18:03

- 开始阶段 7 Android 地图展示。
- 对照 Google Maps Android 当前官方 Compose 接入方式，采用 Maps Compose，避免手动维护传统 `MapView` 生命周期。
- Android 构建脚本新增 Google Maps key 注入，读取顺序为 Gradle 属性、环境变量、本机 `.env`，不把密钥写入源码。
- Android Manifest 新增 `com.google.android.geo.API_KEY` 元数据。
- 新增 Android 地图页和列表页切换，登录后默认进入地图页。
- 地图页绘制轨迹点 Marker 和轨迹 Polyline。
- 点击 Marker、Polyline 或列表项会选中轨迹，选中轨迹使用更高层级和强调色。
- 地图视野优先适配选中轨迹，没有选中轨迹时适配全部轨迹点，没有轨迹时显示中国范围默认视野。
- 第一次构建因为 `matchParentSize` 和 `align` 使用在错误 Compose 作用域中失败，改用 `fillMaxSize` 并将地图角落提示改为 `BoxScope` 组件。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 成功。

## 2026-07-02 18:38

- 开始阶段 8 航班和火车导入。
- 验证 FlightRadar24 Explorer token 可访问 `flight-summary/full` 和 `live/flight-positions/full`，请求需要 `Authorization: Bearer`、`Accept-Version: v1` 和 `Accept: application/json`。
- 验证 12306 当前日期查询链路仍为先调用 `search/v1/train/search` 获取内部 `train_no`，再调用 `queryTrainInfo/query` 获取经停站。
- 验证 Google Geocoding 可解析火车站和机场坐标。
- 新增 `backend/migrations/004_import_records.sql`，记录外部导入请求和响应。
- 新增后端导入适配器，航班导入使用 FlightRadar24 航班摘要、起降机场坐标和可用的实时位置生成近似轨迹，火车导入使用 12306 经停站和 Google Geocoding 生成近似轨迹。
- 新增 `POST /api/import/flight` 和 `POST /api/import/train`，导入成功后一次性写入轨迹片段、轨迹点和导入记录。
- 新增后端测试覆盖航班导入成功、火车导入成功和外部导入失败不写入轨迹。
- Web 航班和火车导入面板改为调用真实后端接口，导入成功后把新轨迹插入列表并回到地图页。
- Android 新增航班导入页和火车导入页，导入成功后回到地图页并选中新轨迹。
- 将本机 FlightRadar24 token 写入 `.env` 的 `FR24_API_TOKEN`，未在终端输出和文档中记录密钥内容。
- 第一次 Android 阶段 8 构建失败，原因是主页面 `if/else` 分支连接错误；改为 `when (state.view)` 后构建通过。
- 执行 `python -m app.migrate && python -m pytest -q` 通过，结果为 `10 passed, 1 warning`。
- 执行 `npm run build` 通过，并将 Web 构建产物部署到 `/var/www/semap/`。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 真实 HTTP smoke 使用临时账号调用 `POST /api/import/train` 导入 `G803`，返回 14 个点；调用 `POST /api/import/flight` 导入 `UA938`，返回 3 个点；临时账号已清理。
- Playwright 使用部署后的 Web 页面完成注册、进入火车导入、提交 `G803`、返回轨迹地图和显示 Google Maps；控制台错误和警告为空。
- Playwright 截图文件保存在 `/tmp/semap-stage8-web-train-import.png`。
- 补充外部服务请求错误收敛，网络错误和非 JSON 响应统一转成中文 `ImportFailure`。
- 重启 `semap-backend` 后再次调用真实导入 smoke，`G803` 返回 14 个点，`UA938` 返回 3 个点。
- Android 主界面模式按钮拆成两行，避免窄屏上一行放不下；重新执行 `./gradlew --no-daemon :app:assembleDebug` 通过。

## 2026-07-02 18:58

- 用户明确火车导入需要输入日期和用户乘车起止点。
- 用户明确中间站不需要在地图上标记，只保留轨迹折线。
- 重新验证 12306 指定日期链路，`G803` 在 `2026-07-02` 返回 14 个经停站，包含 `北京南` 和 `上海虹桥`。
- 后端 `POST /api/import/train` 新增 `date`、`fromStation`、`toStation` 字段。
- 后端火车导入改为使用用户输入日期查询 12306，并按用户乘车起止站截取经停站列表。
- 后端火车导入只给起点和终点保留点位 `name`，中间站坐标继续写入点位用于轨迹线。
- Web 火车导入表单新增日期、乘车起点和乘车终点。
- Android 火车导入页新增日期、乘车起点和乘车终点。
- Web 和 Android 地图改为火车轨迹只给首尾点绘制 Marker，其他类型只给有 `name` 的点绘制 Marker，轨迹线仍使用全部点。
- 新增测试覆盖火车经停站区间截取。
- 执行 `python -m pytest -q` 通过，结果为 `11 passed, 1 warning`。
- 执行 `npm run build` 通过，并将 Web 构建产物部署到 `/var/www/semap/`。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 真实 HTTP smoke 使用 `G803`、`2026-07-02`、`廊坊` 到 `济南西`，返回 3 个点，点位名称为 `廊坊`、空、`济南西`；反向区间返回中文 `400`。
- Playwright 使用部署后的 Web 页面完成火车导入表单验证，地图 `.mapMarker` 数量为 2，控制台错误和警告为空。
- Playwright 截图文件保存在 `/tmp/semap-train-range-web.png`。
- 兼容旧火车轨迹数据，即使中间站已有 `name`，地图也只显示首尾 Marker。
- 重新执行 `npm run build`、`python -m pytest -q` 和 `./gradlew --no-daemon :app:assembleDebug` 均通过，最新 Web 构建已部署。

## 2026-07-02 18:56

- 删除 Web 和 Android 导入页的自定义标题输入。
- 后端航班导入标题统一生成为 `{航班号} 航班`。
- 后端火车导入标题统一生成为 `{车次号} {乘车起点}到{乘车终点}`。
- 删除航班和火车导入请求模型中的 `title` 字段，Web、Android 和测试请求体同步删除该字段。
- 细化外部导入错误返回，包含请求超时、HTTP 状态、非 JSON 响应、Google Geocoding 返回详情、12306 搜索和经停站查询失败详情。
- 火车导入在用户输入日期无可用车次时，自动尝试服务器当前日期起未来 7 天。
- 火车 fallback 使用查到的车次和时刻信息，返回摘要和点位日期仍使用用户输入日期。
- 新增测试覆盖火车 fallback 日期顺序和用户输入日期映射。
- 第一次真实 smoke 脚本误把多行 `.env` 当 shell 加载，已改为不在脚本中 source `.env`。
- 第二次真实 smoke 脚本按 `200` 判断注册成功，实际注册接口返回 `201`，已改为接受 `201`。
- 执行 `python -m pytest -q` 通过，结果为 `13 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 将 Web 构建产物部署到 `/var/www/semap/`，并重启 `semap-backend`。
- 真实 HTTP smoke 验证 `G803`、`2026-07-02`、`廊坊` 到 `济南西` 无需标题字段导入成功，返回标题为 `G803 廊坊到济南西`，点位名称为 `廊坊`、空、`济南西`。
- 真实 HTTP smoke 验证 `G803`、`2020-01-01`、`廊坊` 到 `济南西` 触发 fallback 后导入成功，摘要和时间使用 `2020-01-01`。
- 真实 HTTP smoke 验证不存在乘车起点时返回 `404`，错误信息包含可用经停站列表。
- Playwright 桌面视口验证火车导入页标题输入数量为 `0`，导入成功后地图 marker 数为 `2`，控制台错误和警告为空，截图保存在 `/tmp/semap-import-no-title.png` 和 `/tmp/semap-import-map-result.png`。
- Playwright 移动视口验证火车导入页标题输入数量为 `0`，控制台错误和警告为空，截图保存在 `/tmp/semap-import-no-title-mobile.png`。
- 执行 `git diff --check` 通过。

## 2026-07-02 19:12

- 用户反馈火车站位置查询可能存在错误点。
- 检查当前实现确认火车站坐标按 `"{station}站 中国"` 调用 Google Geocoding，并直接取第一个结果。
- 检查已有数据发现 `云梦东` 被解析为中国国家级结果坐标，说明该查询方式会触发宽泛地理结果。
- 按用户要求将火车站坐标查询改为直接使用 `"{station}站"`。
- 新增测试断言火车导入传给 geocode 的地址为 `廊坊站` 和 `济南西站`。

## 2026-07-02 19:21

- 开始维护本地火车站坐标表。
- 新增 `backend/app/train_station_coordinates.py`，先收录 `襄阳东`、`安陆西`、`云梦东`、`孝感东` 和 `汉口`。
- 火车导入坐标解析改为本地表优先，未命中本地表时再调用 Google Geocoding。
- 本地表命中时点位 raw 中记录 `source=local_train_station_coordinates`。
- 新增测试覆盖带 `站` 后缀的本地表命中，确认本地表命中时不调用 Google Geocoding。
- 执行 `python -m pytest -q` 通过，结果为 `14 passed, 1 warning`。
- 重启 `semap-backend` 后使用临时账号真实导入 `G6788`、`2026-07-02`、`襄阳东` 到 `汉口`，返回 5 个点，全部命中本地表。
- 真实导入中 `云梦东` 返回坐标为 `31.0479946, 113.7788677`，不再返回中国国家级坐标。
- 执行 `git diff --check` 通过。

## 2026-07-02 19:39

- 用户要求航班轨迹显示机型、飞机注册号、航空公司和航空公司 logo。
- 用户要求铁路轨迹显示担当车型，并从 rail.re 读取第一条担当记录。
- 用户要求铁路轨迹显示中国铁路 logo。
- 验证 `https://api.rail.re/train/G803` 返回数组，第一条记录包含 `emu_no=CR400BFB5154`。
- 验证 `https://api.rail.re/train/G6788` 返回数组，第一条记录包含 `emu_no=CRH380A2723`。
- 验证 FlightRadar24 对 `UA938` 返回 `type=B763`、`reg=N671UA`、`painted_as=UAL` 和 `operating_as=UAL`。
- 新增 `track_segments.metadata` JSONB 字段，用于保存外部导入展示信息。
- 航班导入写入 `vehicleModel`、`registration`、`operatorName`、`operatorCode`、`logoKind`、`logoUrl` 和 `logoText`。
- 火车导入调用 `https://api.rail.re/train/{trainCode}`，使用第一条记录的 `emu_no` 归并生成担当车型。
- 火车导入写入中国铁路运营方、车组号、担当车型和 `CR` logo fallback。
- Web 轨迹列表和详情增加 logo、运营方、机型、注册号、担当车型和车组号展示。
- Android 轨迹列表和地图详情增加 logo、运营方、机型、注册号、担当车型和车组号展示。
- 执行 `python -m app.migrate && python -m pytest -q` 通过，结果为 `15 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- Android 初版只显示文字 fallback；随后加入 Coil Compose，让航空公司远程 logo 能在 App 内加载，加载失败时仍显示文字 fallback。
- 重新执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 将 Web 构建产物部署到 `/var/www/semap/`，并重启 `semap-backend`。
- 真实 HTTP smoke 使用临时账号导入 `UA938`，返回 metadata 包含 `vehicleModel=B763`、`registration=N671UA`、`operatorName=UAL` 和 `logoUrl`。
- 真实 HTTP smoke 使用临时账号导入 `G803`、`2026-07-02`、`廊坊` 到 `济南西`，返回 metadata 包含 `vehicleModel=CR400`、`unitNo=CR400BFB5154` 和 `operatorName=中国铁路`。
- 第一次 Playwright 验证脚本用精确文本选择器时命中列表和详情两个元素，触发 strict mode 报错；改为取第一个匹配项后通过。
- Playwright 验证 Web 页面显示 `担当车型：CR400`、`运营方：中国铁路`、`机型：B763` 和 `注册号：N671UA`，控制台错误和警告为空，截图保存在 `/tmp/semap-metadata-web.png`。
- 验证 `http://127.0.0.1:8000/api/health` 和 `https://semap.xyz/api/health` 均返回数据库正常。
- 执行 `git diff --check` 通过。

## 2026-07-02 20:05

- 用户要求去掉轨迹卡片里的轨迹点数量显示。
- 用户要求航班导入标题改为 `{航班号} {起飞机场}-{降落机场}`，例如 `CZ1145 WUH-CAN`。
- 用户要求航班详细信息增加起飞机场和降落机场。
- 用户要求 Web 轨迹列表允许没有选中项，未选中卡片只显示一行缩略信息。
- 用户要求 Web 选中卡片后才显示详细信息和编辑界面，点击列表栏之外取消选中。
- 用户要求删除备注信息。
- 用户要求火车 logo 改为 12306 logo。
- 新增迁移 `006_remove_segment_note.sql`，删除 `track_segments.note`。
- 后端航班导入标题改为航班号和起降机场代码组合，metadata 增加 `originAirport` 和 `destinationAirport`。
- 后端火车 metadata 的 logo 字段改为 `logoKind=railway_12306` 和 `logoText=12306`。
- Web 去掉轨迹点数量显示，未选中轨迹卡片只显示 logo、类型和标题。
- Web 取消默认选中第一条轨迹，点击地图或列表外区域会取消当前选中。
- Web 和 Android 删除备注字段显示和提交路径。
- Android 去掉轨迹点数量显示，并增加起降机场和 12306 logo fallback 展示。

## 2026-07-02 20:18

- 用户要求 12306 显示官网 logo 图片，不显示纯 `12306` 数字。
- 用户要求火车标题格式改为 `{车次} {起点}-{终点}`，缩略卡片显示日期。
- 用户要求点击页面中轨迹列表以外的区域都取消选中。
- 用户要求删除 Web 手动编辑轨迹入口。
- 用户要求航班详细信息中的起降机场改为起降地点，并使用 IATA code-search 查询城市和机场名。
- 验证 `https://www.12306.cn/index/` 首页 logo 来自 `https://www.12306.cn/index/images/logo.png`。
- 验证 IATA code-search 页面结果表包含 `City Name`、`Airport Name` 和 `3-letter location code`，`CAN` 对应 `Guangzhou Baiyun Intl`。
- 后端新增 IATA 机场地点解析，将城市名和机场名拼接写入 `originLocation` 和 `destinationLocation`，查询失败时回退到 IATA 代码。
- 后端火车导入标题改为连字符区间格式，火车 metadata 写入 12306 官网 logo URL。
- Web 删除轨迹编辑和删除表单，选中轨迹后只在卡片中展开详情。
- Web 缩略卡片增加日期，点击轨迹列表以外的页面区域会取消当前选中。
- Android 初始不默认选中轨迹，火车缩略行显示日期，航班详情显示起飞地点和降落地点。
- 执行 `python -m pytest -q` 通过，结果为 `16 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 重启 `semap-backend` 并部署 Web 构建产物到 `/var/www/semap/`。
- 清空账号、轨迹、轨迹点和导入记录测试数据后，使用临时账号真实导入 `UA938` 和 `G803`。
- 真实 HTTP smoke 验证 `UA938` 返回标题 `UA938 ORD-LHR`，起飞地点 `Chicago O'Hare International`，降落地点 `London Heathrow`。
- 真实 HTTP smoke 验证 `G803` 返回标题 `G803 北京南-上海虹桥`，`logoUrl=https://www.12306.cn/index/images/logo.png`，担当车型 `CR400`，中间站不保留地图 Marker 名称。
- Playwright 验证公网 Web 初始无选中卡片，卡片显示日期且不显示点数，选中航班后显示起飞地点和降落地点，点击工具栏取消选中，选中火车后 12306 官网 logo 图片加载成功，保存和删除按钮不存在，控制台错误和警告为空。
- Playwright 截图文件保存在 `/tmp/semap-current-initial.png`、`/tmp/semap-current-flight-selected.png`、`/tmp/semap-current-cleared.png`、`/tmp/semap-current-train-selected.png` 和 `/tmp/semap-current-mobile.png`。

## 2026-07-02 20:40

- 用户要求铁路 logo 使用项目 `logos` 目录中的中国铁路图标。
- 将中国铁路 SVG 放入 Web 静态资源目录 `frontend/public/logos/China_Railways.svg`，生产部署后通过 `/logos/China_Railways.svg` 访问。
- 后端火车 metadata 的 `logoUrl` 改为 `/logos/China_Railways.svg`。
- Web 和 Android 对 `logoKind=railway_12306` 强制使用本地中国铁路 SVG。
- Web 和 Android 详细卡片增加火车出发地点、到达地点、所有轨迹出发时间和到达时间。
- Web 和 Android 时间格式固定为中国时区的 `yyyy/MM/dd HH:mm`。
- 去掉 Web 详细卡片底部原有单独时间栏。
- 用户反馈中国铁路 logo 左右留白过大后，将 Web 和 Android 轨迹卡片 logo 容器改为正方形，图片按正方形图标居中显示。
- 清理一次失败验证脚本留下的 `qa_cards_%` 临时账号。
- 执行 `npm run build`、`python -m pytest -q` 和 `./gradlew --no-daemon :app:assembleDebug` 均通过。
- 将 Web 构建产物部署到 `/var/www/semap/`，并重启 `semap-backend`。
- Playwright 验证铁路 logo 容器为 `40x40`，图片为 `32x32`，资源为 `/logos/China_Railways.svg`。
- Playwright 验证火车详情显示 `出发地点：北京南站`、`到达地点：上海虹桥站`、`出发时间：2026/07/01 12:12` 和 `到达时间：2026/07/01 18:18`。
- Playwright 验证航班详情显示起飞地点、降落地点和出发时间，控制台错误和警告为空。
- 截图文件保存在 `/tmp/semap-square-railway-logo.png` 和 `/tmp/semap-flight-time-location.png`。
- 清理 Playwright 验证使用的 `qa_cards_current` 临时账号。

## 2026-07-02 21:12

- 开始阶段 9 GPS 轨迹记录。
- 新增后端迁移 `007_location_sessions.sql`，创建 `location_sessions` 表。
- 后端新增 GPS 定位会话接口：开始、上传点、暂停、继续和结束。
- 开始定位会话时创建 `gps` 轨迹片段，点位上传按轨迹当前最大序号追加。
- 暂停状态上传点返回 `409`，结束时写入轨迹结束时间和摘要。
- 新增后端测试覆盖定位会话完整流程、暂停拒绝点位、继续后恢复上传。
- Android 新增 Room `pending_location_points` 表，用于短时缓存待上传定位点。
- Android 新增前台定位服务 `GpsRecordingService`，支持开始、暂停、继续、结束、缓存点位和恢复后补传。
- 初次实现曾尝试引入 Google Play services location，`mergeExtDexDebug` 阶段多次因为服务器内存不足导致 Gradle daemon 被 OOM kill。
- 移除 Play services location 依赖，改用 Android 系统 `LocationManager` 的 GPS 和网络定位提供方。
- Gradle heap 从 1536m 调回 1024m，并停止残留 Kotlin daemon 后，Debug APK 构建通过。
- Android Manifest 增加 `ACCESS_FINE_LOCATION`、`ACCESS_COARSE_LOCATION`、`FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_LOCATION` 和 `POST_NOTIFICATIONS` 权限。
- Android 默认 API 地址改为 `https://semap.xyz/api/`，便于 Web 下载 APK 安装后直接连接公网后端。
- Web 登录页和登录后的侧栏新增 Android APK 下载入口，链接为 `/downloads/semap-debug.apk`。
- 构建 APK 后复制到 `/var/www/semap/downloads/semap-debug.apk`，公网 `HEAD` 返回 `200 OK`，大小约 14 MB。
- 执行 `python -m pytest -q` 通过，结果为 `18 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 执行后端迁移并重启 `semap-backend`，重启瞬间公网健康检查返回一次 `502`，随后本机和公网健康检查均恢复正常。
- 当前环境没有真实 Android 手机，未做真机 GPS 定位记录验收。

## 2026-07-02 21:32

- 用户反馈 Web 轨迹列表右栏没有用满高度，并且满列表状态下选中卡片会变窄。
- 移除轨迹列表 `max-height: 300px` 限制，让右栏列表区域占满地图同高的剩余空间。
- 宽屏两栏布局将主界面固定在视口内，轨迹列表在右栏内部滚动；窄屏和移动端继续使用自然页面滚动。
- 为轨迹列表滚动条预留稳定 gutter，避免滚动条出现时挤压卡片宽度。
- 将轨迹卡片宿主从 `button` 改为 `div role=button`，保留 Enter 和 Space 键选择能力。
- 将轨迹列表容器从 CSS grid 改为纵向 flex，并给轨迹卡片设置 `flex: 0 0 auto`，避免满列表时展开详情被压回最小高度。
- 使用临时账号 `qa_list_211905` 插入 18 条测试轨迹复现满列表状态。
- Playwright 验证右栏高度和地图高度一致，列表内部滚动，选中前后卡片宽度变化为 0，选中详情包含在卡片内部，控制台错误和警告为空。
- 验证截图保存在 `/tmp/semap-list-layout-fixed.png`。
- 清理临时账号 `qa_list_211905`，相关轨迹和点位已级联删除。

## 2026-07-02 21:50

- 用户反馈火车导入查询站点后页面没有滚动条，导入按钮在当前缩放比例下不可达。
- 用户要求航班和火车导入卡片在大屏页面中央显示。
- 给 Web 工作区增加 `tracksShell` 和 `importShell` 状态类。
- 轨迹页继续使用固定视口布局，导入页改为工作区内部纵向滚动。
- 新增 `importStage` 居中容器，航班和火车导入卡片在工作区水平居中，内容较短时垂直居中，内容较长时可滚动访问底部按钮。
- 执行 `npm run build` 通过。
- Playwright 使用本地 Vite dev 验证 1280x640 视口，火车查询站点后 `.workspace` 的 `scrollHeight=800`、`clientHeight=640`，滚动到底后导入按钮位于 `575-617px`，可见。
- Playwright 验证火车和航班导入卡片相对工作区水平中心偏移为 `0px`。
- Playwright 截图保存在 `/tmp/semap-import-scroll-train.png` 和 `/tmp/semap-import-centered-flight.png`。
- 本地验证时初始轨迹地图请求 Google Maps 脚本返回 404，和导入页布局无关。

## 2026-07-02 22:18

- 用户要求 GPS 图标改为 `logos/road.png`。
- 用户要求 GPS 轨迹标题改为 `定位上传 1` 这种格式。
- 用户要求在详细卡片右下角提供“更改”按钮，点击后通过弹窗式卡片修改标题和起止时间，并提供保存和删除路径功能。
- 将 `logos/road.png` 复制到 `frontend/public/logos/road.png` 和 Android `drawable-nodpi/road.png`。
- 后端创建 GPS 会话时按当前用户已有 GPS 轨迹数量生成 `定位上传 N` 标题，并写入 `logoKind=gps_road`、`logoUrl=/logos/road.png` 和 `logoText=GPS`。
- Web 端旧 GPS 记录即使没有 metadata，也按 `sourceType=gps` 显示 `/logos/road.png`。
- Android 端 GPS 轨迹卡片直接使用本地 `R.drawable.road`。
- Web 选中轨迹后在详细卡片右下角显示“更改”，弹窗内调用 `PATCH /api/segments/{segmentId}` 保存标题、出发时间和到达时间，调用 `DELETE /api/segments/{segmentId}` 删除路径。
- 新增后端测试断言 GPS 标题和 road metadata。
- 执行 `python -m pytest -q` 通过，结果为 `20 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。

## 2026-07-02 22:21

- 用户反馈 Android 端存在页面无法滚动、GPS 记录出现异常记录和 GPS 定位精度低的问题。
- Android 登录页、航班导入页、火车导入页和 GPS 记录页改为可纵向滚动，轨迹列表页显式填满内容区后由 `LazyColumn` 自身滚动。
- Android GPS 记录增加 `starting` 状态，开始记录在启动中、记录中和暂停状态下保持幂等，避免重复点击或服务重复启动创建多个定位会话。
- Android GPS 前台服务从 `START_STICKY` 改为 `START_NOT_STICKY`，避免系统无原始 intent 重启服务后继续旧状态。
- Android GPS 开始会话前先检查精确定位权限和系统 GPS provider，权限缺失或 GPS 未开启时不创建后端轨迹。
- Android GPS 定位来源改为只订阅系统 `GPS_PROVIDER`，不再使用网络定位 provider。
- Android GPS 点位上传前过滤无精度信息、非 GPS provider 和精度误差超过 50 米的定位点，等待高精度信号时在记录页显示提示。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 将新的 Debug APK 复制到 `/var/www/semap/downloads/semap-debug.apk`，公网 `HEAD` 返回 `200 OK`，大小约 15 MB。
- 当前环境没有真实 Android 手机，无法现场验证实际 GPS 轨迹质量。

## 2026-07-02 22:29

- 用户要求 Web 详细卡片中航班按航班号、运营方、机型、注册号、起飞地点、降落地点、出发时间、到达时间逐行显示。
- 用户要求航班运营方通过 IATA code-search 按航司二字码查询，例如 `UA` 显示为 `United Airlines Inc`。

## 2026-07-02 22:41

- 用户指出航班运营方不能使用本地兜底，需要修正 IATA 页面请求并确保正常检索。
- 删除 `UA` 本地兜底表，后端改为真实请求 IATA `airline.search` 并跟随跳转。
- IATA 航司查询失败或没有匹配时，航班导入返回错误，不再静默写入 `UAL` 或航司代码作为运营方。
- 使用后端代码真实执行 `iata_airline_name("UA")`，返回 `United Airlines Inc`。
- 用户要求航班机型和注册号放在同一行，Web 和 Android 均改为 `机型：B763　注册号：N674UA`。
- Android 地图页改为可纵向滚动页面，地图区域使用固定高度，详情卡片不会被屏幕底部裁掉。
- Android 轨迹详情去掉 `summary` 描述输出。
- Android 中国铁路 logo 改为本地 VectorDrawable，航司远程 logo 加载失败时显示文字 fallback。
- 执行 `python -m pytest -q` 通过，结果为 `22 passed, 1 warning`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 部署 Web 构建产物、重启 `semap-backend`，并更新 `/var/www/semap/downloads/semap-debug.apk`。
- Playwright 使用临时账号验证 Web 航班详情显示 `运营方：United Airlines Inc` 和 `机型：B763　注册号：N674UA`，火车详情不显示运营方和车组号，控制台错误和警告为空。

## 2026-07-02 22:58

- 用户反馈真机运行时无法成功上传定位点，但其他地图软件可以精确定位。
- 排查 Android `GpsRecordingService`，当前代码只订阅系统 `GPS_PROVIDER`，并在上传前要求 provider 必须等于 `gps` 且精度误差不超过 50 米。
- 判断无法上传的主要原因是 App 把 Google Play services 融合定位、网络辅助定位和室内冷启动产生的可用定位点全部过滤掉。
- Android 重新引入 `com.google.android.gms:play-services-location:21.4.0`。
- Android GPS 记录改用 `FusedLocationProviderClient` 请求 `PRIORITY_HIGH_ACCURACY` 连续定位，保留 5 秒采样间隔、5 米最小位移和 50 米精度过滤。
- Android 开始记录前改为检查系统定位总开关，不再强制要求系统 GPS provider 单独开启。
- Android 等待高精度定位时显示当前定位误差，收到合格点位后清除等待提示。
- Android 根界面增加安全区 padding，避开系统状态栏和导航栏。

## 2026-07-02 23:04

- 用户反馈手机端航司 logo 无法加载，询问是否可以改为后端获取航司 logo 后提供给客户端。
- 排查当前实现，航班 metadata 的 `logoUrl` 直接写入 `https://images.kiwi.com/airlines/64/{code}.png`，Android 端直接访问第三方图片源。
- 后端新增 `/api/assets/airline-logos/{code}.png`，按航司二字码获取第三方 PNG 并缓存到 `/var/cache/semap/airline_logos/`。
- 航班 metadata 的 `logoUrl` 改为项目 API 路径 `/api/assets/airline-logos/{code}.png`，Web 和 Android 都通过 SEMAP 后端获取 logo。
- 现场验证 `UA` logo 成功缓存为 64x64 PNG，路径为 `/var/cache/semap/airline_logos/UA.png`。
- 将数据库中 10 条已有航班轨迹的 `metadata.logoUrl` 更新为后端代理路径。
- 航司 logo 接口同时支持 GET 和 HEAD，便于浏览器加载和命令行检查。

## 2026-07-02 23:22

- 用户反馈手机端航司 logo 仍无法加载，并且 GPS 定位在地图上存在巨大误差。
- 验证公网 `https://semap.xyz/api/assets/airline-logos/UA.png` 返回 `200 OK` 和 `image/png`，后端 logo 代理可用。
- 排查 Android 依赖，Coil 3 只有 `coil-compose`，缺少 HTTP 网络加载模块。
- Android 增加 `io.coil-kt.coil3:coil-network-okhttp:3.4.0`，APK 内可见 Coil fetcher service。
- 排查生产库旧 GPS 点，22 个 Android GPS 点的 raw 只有 `source=android_gps`，没有坐标系信息。
- 在武汉样例坐标上计算 WGS84 到地图展示坐标的偏移，约为南向 260 米、东向 520 米，和用户看到的大偏差一致。
- Android GPS 上传前增加过期定位点和模拟定位点过滤。
- Android GPS 在中国大陆范围内把原始 WGS84 坐标转换为地图展示坐标后上传，Room 缓存和接口请求同时保留 `accuracy`、`provider`、`rawLat`、`rawLng` 和 `coordinateSystem`。
- 后端定位点接口接收并保存 `accuracy`、`provider`、`rawLat`、`rawLng` 和 `coordinateSystem` 到点位 raw 字段。
- 对生产库 22 个旧 Android GPS 点做一次定向转换，把原 lat/lng 写入 raw 的 `rawLat` 和 `rawLng`，展示坐标更新为 GCJ-02，并写入 `coordinateSystem=gcj02`。
- 执行 `python -m pytest -q` 通过，结果为 `24 passed, 1 warning`。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 重启 `semap-backend`，公网 `https://semap.xyz/api/health` 返回 `{"status":"ok","database":"ok"}`。
- 将新版 Debug APK 复制到 `/var/www/semap/downloads/semap-debug.apk`，公网下载入口返回 `200 OK`，大小约 21 MB。

## 2026-07-02 23:36

- 用户要求 Web 左栏名称改为“轨迹地图”“航班导入”“火车导入”。
- 用户要求 APK 下载入口增加提示，说明手机端有定位上传功能。
- 用户要求去掉工具栏的“n 条轨迹”副标题，去掉导入卡片的 `FlightRadar24` 和 `12306 指定日期` 副标题。
- 用户要求导入表单不带默认字段。
- Web 端导入日期不再默认填入当天日期，航班号和车次号输入框不再显示示例占位。
- 用户要求以当前版本作为 APK 第一个正式版本并开始版本维护。
- Android `versionName` 更新为 `1.0`，`versionCode` 更新为 `100`。
- Web APK 下载入口改为 `/downloads/SEMAP-1.0.apk`。
- 执行 `npm run build` 通过。
- 执行 `./gradlew --no-daemon :app:assembleDebug` 通过。
- 将 Web 构建产物部署到 `/var/www/semap/`。
- 将 APK 复制为 `/var/www/semap/downloads/SEMAP-1.0.apk`，公网 `HEAD` 返回 `200 OK`。
- Playwright 验证登录页显示定位上传提示，登录后左栏显示“轨迹地图”“航班导入”“火车导入”，工具栏不显示轨迹数，航班和火车导入卡片不显示副标题，导入表单初始值为空，控制台错误和警告为空。
- Playwright 截图保存在 `/tmp/semap-web-apk-login.png`、`/tmp/semap-web-flight-import.png` 和 `/tmp/semap-web-train-import.png`。

## 2026-07-02 23:48

- 用户指出火车担当车型显示值不合理，要求改成 rail.re 原始值去掉最后四位。
- 后端 `train_vehicle_model` 改为对 `emu_no` 做统一截断，`CR400BFB5154` 显示为 `CR400BFB`，`CRH380A2723` 显示为 `CRH380A`。
- 完整 `emu_no` 继续保存在 metadata 的 `unitNo` 字段。
